[project]
name = "hgmiss"
version = "1.0.0"
requires-python = ">=3.12"
description = "Code for: What a Violated Missingness Assumption Costs"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
